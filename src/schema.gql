# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

input CreateSubjectInput {
  grades: Float!
  name: String!
}

input CreateTeacherInput {
  age: Int!
  name: String!
  subjectId: String!
}

type Mutation {
  createSubject(data: CreateSubjectInput!): Subject!
  createTeacher(data: CreateTeacherInput!): Teacher!
  deleteSubject(id: String!): Subject!
  updateGrades(data: UpdateGradesInput!): Subject!
}

type Query {
  getSubjectById(id: String!): Subject!
  getSubjects: [Subject!]!
  getTeacherById(id: String!): Teacher!
  getTeacherBySubjectId(subjectId: String!): Teacher!
  getTeachers: [Teacher!]!
}

type Subject {
  grades: Float!
  id: ID!
  name: String!
  teacher: Teacher!
}

type Teacher {
  age: Int!
  id: ID!
  name: String!
  subject: Subject!
  subjectId: String!
}

input UpdateGradesInput {
  grades: Float!
  id: String!
}
